<?php ?> - u ovome se pise

Mora ;!!!

promenljive pocinju sa $

$var = [...];

Nizovi:
$niz = [1, 2, 3];
$niz = array(1, 2, 3);
$niz[] = 4; - dodavanje na poslednje mesto u nizu

Petlje:
foreach($niz as $n) {
  [...]
}

Funkcije
Ugradjene:
var_dump($prom); - vraca vrednost i tip promenljive.
print_r($prom); - samo vrednost

CLSX - kondicionalni rad sa classNames

const className = clsx({
    correct: isCorrect,
    wrong: isWrong
})

const className = clsx("chip", isLanguageLost && "lost")

const gameStatusClass = clsx("game-status", {
    won: isGameWon,
    lost: isGameLost
})

a11y - accessability