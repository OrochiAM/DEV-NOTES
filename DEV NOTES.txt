*CMD
cd - change dir
ls - list files
pwd - current working dir

echo - ispis
> - new
>> - append

CLI > SHELL > OS

mkdir - kreiranje foldera
rmdir - brisanje praznog foldera
rm -r [dir] - brisanje punog foldera

find .(trenutni folder) -name (by name) 'string*'

. - current dir
.. - parent dir
~ - home dir
/ - root dir

mv [old] [new] - rename
   [name] [new_location] - move

cp [old_name] [copy_name] - copy
   -r - za dir

grep [pattern] [file(s)] - search in file
     'string' dir* - primer
     -n - line number

sed 's/pattern/replacement/' [file] - replace in files
    's/,/:' - primer
    '[...][options]'

wc [file] - count words
   -l, -w, -c, -m : options

sort [file]
     -r: obrnuto
     -n: brojevi

uniq [file] bez duplikata

| - pipe character
    [cmd1] | [cmd2]


*ACCESSIBLE DEVELOPMENT
accessability tree - pregled pristupacnosti

WCAG 2.0 - pristupacnost guideline

A - not
AA - pristupacno 
AAA - potpuno pristupacno

BOJE
Kontrast
crna:bela = 1:21

AA:
4.5:1 - normalan tekst
3:1 - veci tekst

AAA:
7:1 - normal
4.5:1 - veliki tekst

ARIA LABELS UMESTO DOSTA STVARI

aria-live="polite..." - ako se menja dinamicki kroz JS

h1-h6 samo za naslove koji oznacavaju deo sajta
p za drugi tekst 

*HTML

ide h1 h2... ne preskaci

Data attribute
data-[naziv]="podaci"

Forme:

<form onsubmit="return false">

<fieldset> 
	<legend>Legenda</legend>
</fieldset> - grupisanje polja u formi.

<label for="[ID]"> - label ide na ID!

<input type="submit" /> - ista stvar, button bolji za dizajn.
<button type="submit">,
        type="reset" - reset podataka
        type="button" - da ne bude submit u formi

<input type=[...] required> - mora da bude popunjen
                  disabled

pattern="[a-zA-Z0-9]+" - za inpute regex (plus znaci da je bilo koje duzine) 
         [a-zA-Z]{3} - 3 je zahtevani broj karaktera
minlength="[...]" - duzina stringa input tezt
maxlength="[...]"
min="21" - brojevi
max="120"

<input type="file" required accept="image/png, image/jpg" aria-label="Choose file">

Radio inputi isti name da imaju, i checkbox-ovi

<select id="superpowers" multiple size="67">
  <optgroup label="????">

Semantic HTML:

*CSS
Position fixed sa margin centriranjem mora da ima top, right, bottom, left na nuli.
Margin auto radi samo sa display block, slike imaju display inline.

: - pseudo selektor
:: - pseudo element

hsl([h] [s]% [l]% / [opacity]) - obavezno koristi crticu
hsl(from var(--white) h s l) - h s l su kao promenljive, moze calc i sve tako

Gradient border:
background-image: linear-gradient([...]), url([...]) - sa linear-gradient se lako postize overlay 
background: linear-gradient([...]) padding-box, conic-gradient([...]) border-box - gradient border (za razliku od border image moze radius)

Property primer:
@property --angle {
  syntax: "<angle>";
  initial-value: 0turn;
  inherits: true;
} - custom vrednost koju animiramo

@keyframes rotate {
  to {
    --angle: 1turn;
  }
}

Kul kombo:
background-color,
background-blend-mode

inset shorthand za position.

corner-shape (za sada samo chrome)

accent-color
 
inline elementi nemaju padding i margine

Slike - block
Link - inline

FORM ELEMENTI NE NASLEDJUJU FONT
font-family: inherit;

margine na tekstu sklanjaju display block ako je fleks element

Responsive Design

Relative Units

% - procenat na osnovu roditelja
width override-uje display block

em  
FONTOVI - vrednost u odnosu na velicinu fonta roditelja ili najblizeg rodjaka sa definisanim font size-om (1em - velicina roditelja da prostis)
MARGINE PADDING - u odnosu na velicinu fonta elementa na koji se postavljaju

rem - root em, uvek u odnosu na root

Kada sta koristiti?

Font - rem
Margine i Padding - em
max-width - px

line-height:
1.5 veci od velicine fonta
koristi unitless vrednosti
line-height: 1.5;

Media Query:
@media (max-width: 499px) {
    [...]
}

Mobile First:
Slike su block!!!
Koristi display: block;

Common Breakpoints:
480px - telefoni
768px - tableti
1024px - laptopovi, manji racunari
1280px - racunari

na telefonu se bez meta taga renderuje odzumiran desktop

vw za scalable text

(kod flex i kod grid margine ne collapsuju)
Flex

Na element
flex-basis: 200px; - koliko treba da rastu elementi
flex-grow: 1;
flex: 1 220px; - shorthand
order: -1(first) 1(last);

Odvoji sve flex containere na pocetku

Grid
display: grid;

grid-template-columns: 5em 1fr auto repeat(7, 1fr);
grid-template: 1fr 3fr 1fr / 1fr 2fr;
grid-column: span 2;

row-gap: .3em;
column-gap: .6em;
gap: .3em .6em;

12 column grid

grid-column: 1 / 13;
grid-column: 1 / -1 (uvek do kraja);
grid-column: 3 / span 7;
grid-column-start: 1;
grid-column-end: 13;

grid-area: main;

grid-template-areas: 
        "head head head head head head head head head head head head"
        "nav nav main main main main main main main aside aside aside"
        "nav nav main main main main main main main aside aside aside"
        "nav nav main main main main main main main aside aside aside"
        "foot foot foot foot foot foot foot foot foot foot foot foot";
        .... - jedan element u grid area

source order independence 

grid-auto-rows: 75px;

grid-auto-flow: dense; nestaje prazan prostor u gridu

grid-template-columns: repeat(auto-fit, minmax(100px, 1fr)); - ne idu ispod 100px, i ne preko 1fr
grid-template-columns: repeat(9, 1fr) repeat(3, minmax(70px, 1fr));

place-items: center; - centrira sve u gridu 

JS

Ne pravi const ako ne moras.
innerHTML ako menjamo HTML u suprotnom innerText.
(innerHTML moze da bude opasan, html injection)

Razliciti fajlovi: 
export const dinnerPartyGuests = []
export { interplanetaryDestinationsArr, shortSpaceTripsArr }
export default [...], moze da se importuje pod bilo kojim imenom (samo jedan po fajlu)

import { dinnerPartyGuests } from '/data.js' (root path)
import { interplanetaryDestinationsArr as destinations } from '/data.js'
imenovani import

import { interplanetaryDestinationsArr as destinations, shortSpaceTripsArr } from '/data.js'
zarezom se mogu vise stvari importovati

<script src="index.js" type="module"></script>
Modularan kod mora da bude na serveru.

Loops:
for of - elementi
for in - indeksi

Forme:
loginForm.addEventListener('submit', function(e){
    e.preventDefault() 
}) - nema default ponasanje (ne samo za forme)

const formData = new FormData(form) - form data u objektu
formData.get([name iz inputa]) - uzimanje objekta

element.disabled = true / false

container.classList.toggle([klasa])

Radio button vrednost najbolje dobiti preko querySelectora (input[type="radio"]:checked), checkbox takodje.

Checkbox.checked 

Eventovi:
IMPORTANT!!! Kad se doda event listener na parenta, e.target radi i za decu, radi svaki event wtf.
emotionRadios.addEventListener('input', function(e) {
    console.log(e.target.id)
})

change - kad se izgubi fokus
input - svaka promena

getElementsByClassName vraca HTMLCollection (automatski se menja kad se dom menja)

Funkcije 
.filter(OVDE IDE FUNKCIJA CUSTOM FILTER EZ)
filter vraca niz

.includes - za proveru NE CONTAINS

.forEach(f-ja, index) - radi nesto na svakom elementu, index vraca index u nizu

.map(f-ja, index) - menja svaki element po f-ji
*forEach ne vraca novi niz, map vraca

.join('[SEPARATOR]') - spaja elemente niza

.reduce() - vraca samo jedan element (izvrsava callback funkciju)

const rainJanuaryByWeek = [ 10, 20, 0, 122 ]

const totalRainfallJanuary = rainJanuaryByWeek.reduce((total(callback), currentElement) => total + currentElement)
.reduce((total, currentElement => total + currentElement), 0)  - inicijalna vrednost total

Hoisting - dekleracije funkcija se pomeraju na pocetak svog scope-a (svuda mogu da se pozovu funkcije)

Function expression (Ne hoistuje se, to je prednost)

One grane koje nemaju return u funkciji vracaju undefined.

Default parametri:
function calculateTotalCost(itemsBoughtArr, discount=0) - ako se ne da vrednost defaultu, postavlja se na nula

Rest parametar
function setPermissionLevel(permissionLevel, ...names) {[...]}
setPermissionLevel('admin', 'Dave', 'Sally')

OOP JS

Objekti sa metodama
const gamer = {
    name: 'Dave',
    score: 0,
    incrementScore: function(){
        this.score++   
    }
}

Konstruktor Funkcije

function Gamer(name){
    this.name = name
    this.score = 0
    this.incrementScore = function(){
        this.score++  
    }
}

Klase 

class Gamer {
    constructor(name, score) {
        this.name = name
        this.score = score
    }
    
    incrementScore() {
        this.score++  
    }
}

Try Catch
    try {
        if (typeof payment === 'number') {
            console.log(payment + 10)
        } else {
            throw new ReferenceError('payment is not a number')
        }
    } catch (err) {
        console.error('Error: ' + err)
    }

Switch

switch(item) {
    case 'coffee':
        price = 2
        break
    case 'sandwiches':
        price = 5
        break    
    default:
        return `Sorry, we don't sell ${item}`         
}

break ne ide posle poslednjeg itema u switchu, switch poredi sa ===

Datumi
const dateSnapshot = new Date()
console.log(dateSnapshot.toString())

getFullYear() -> 2026
getYear() -> 26

Error konstruktor
throw new Error('No username provided')
console.log(new Error('No username provided'))

Spread operator
...[nazivNiza] - vraca sve elemente niza
Koristi se kod Math.max i min

Short Circuiting

OR
const jobHunter = {
    name: 'Tom Chant',
    jobSearchArea: 'Europe',
}
const workLocation = jobHunter.jobSearchArea || 'Worldwide'
console.log(`${jobHunter.name}'s work location is ${workLocation}`)

AND 
const user = {
    userName: 'Tom',
    role: 'admin',
}

user.role === 'admin' && console.log('Dashboard Displayed')

CDN - Content Delivery Network
lista cdn-a - https://cdnjs.com/

Data Attribute
[element].dataset.[naziv]
KORISTI CAMEL CASE IAKO SU CRTICE U HTMLU

Shallow Copy - kreirana kopija niza ili objekta niza menja originalni niz
Deep Copy - ne menja.

Object Destructuring
const {title, year, genre, star, director} = favouriteFilm

APIs, Async

API - nacin da se povezemo sa drugim programom

Client / Server

Request Response Cycle

Internal error: 500
Forbidden: 403
Not Found: 404

XHR (XMLHttpRequest) - JSON AJAX Request

Fetch

fetch('https://dog.ceo/api/breeds/image/random')
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error('Error:', error));